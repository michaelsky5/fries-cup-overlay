FCUP 导播工具项目汇总
1. 项目定位
这是一个基于 React + OBS Browser Source 的赛事导播控制系统，最初服务于 Fries Cup / 守望先锋赛事导播，目标是让导播可以在一个网页控制台里完成：
比赛实时 HUD 控制队伍 / 选手 / 解说信息管理地图池与 BAN 位控制比分、胜负、当前地图切换倒计时、视频、高光、封面管理数据包装图文输出OBS Overlay 页面同步
当前项目本质上是一个：
网页控制台 Console+OBS Overlay 播出层+房间 room 隔离机制+本地持久化数据系统
未来可以抽象成一个 通用赛事导播工具，不只服务守望先锋，也可以服务其他电竞赛事、校园赛、线上联赛、杯赛等。

2. 当前核心架构
2.1 控制台与 Overlay 分离
项目分为两个入口：
控制台：https://console.fries-cup.com/?room=fcup-xxxxOBS Overlay：https://console.fries-cup.com/?room=fcup-xxxx#overlay
控制台负责编辑数据、预览画面、点击 TAKE。
Overlay 负责在 OBS 中显示最终播出画面。
当前正式实用方案暂定为：
OBS 自定义浏览器停靠窗口 = 控制台OBS Browser Source = Overlay两者使用同一个 room
原因是：暂时不做云端 sync-server，不要求每个导播本地运行 Node，也不强制外部 Chrome 控制 OBS 内置 Overlay。

2.2 Program / Preview / TAKE 逻辑
当前核心原则：
matchData.globalScene 是正式播出 Program 的唯一真源Program Monitor 读 matchData.globalSceneOverlay 读 matchData.globalScenePreview 读 previewScenePreview 不参与同步只有 TAKE 才能把 Preview 推到 Program
标准切场逻辑：
setPreviewScene('XXX_SCENE')↓takeScene('XXX_SCENE')↓matchData.globalScene = 'XXX_SCENE'↓Program Monitor 和 Overlay 同步显示
重要规则：
普通 updateData / updateWithHistory 不应该直接写 globalScene子组件上墙必须走 takeSceneUndo / Restore Snapshot 不应该恢复旧 globalScene

3. 当前同步方案
3.1 当前阶段采用 OBS 内部浏览器方案
当前先不做云端同步服务，实际使用方案是：
OBS Dock 控制台+OBS Browser Source Overlay+同一个 room+localStorage / BroadcastChannel fallback
使用示例：
控制台 Dock：https://console.fries-cup.com/?room=fcup-20260510-abcdOBS Overlay：https://console.fries-cup.com/?room=fcup-20260510-abcd#overlay
OBS Browser Source 建议关闭：
Shutdown source when not visibleRefresh browser when scene becomes active
目的是减少 OBS 自动刷新或休眠导致的白屏、丢状态、不同步。

3.2 云端 sync-server 方案暂时搁置
曾经讨论过更完整的方案：
Chrome / Edge 控制台↓https://sync.fries-cup.com↓OBS Overlay
但这需要部署云端 sync-server。由于当前项目优先目标是能稳定跑比赛，所以暂时搁置。
保留未来方向：
公共 Sync Serverroom 隔离SSE / polling 同步导播无需本地运行 Node

3.3 OBS WebSocket 定位
OBS WebSocket 不是当前必须项。
未来可以作为增强模块，用于：
刷新 OBS Browser Source切 OBS 场景开关 OBS Source控制录制 / 推流
但不应该重新承担 Overlay 数据同步。
正确拆法：
网页同步系统：负责 Overlay 内容OBS WebSocket：负责 OBS 软件外壳操作

4. 数据存储设计
4.1 room-scoped 比赛数据
每个房间独立保存比赛数据：
fries_cup_data:<roomId>
例如：
fries_cup_data:fcup-20260510-abcd
应该属于 room 数据的内容：
teamA / teamBscoreA / scoreBplayersA / playersBmapLineupcurrentMapbansA / bansBbanOrderModeshowBanPhasewinnerglobalScenetickerTextactiveVideoPathactiveHighlightPathstatsTemplateDatamapProfileDataplayerComparisonDatateamComparisonDataleaderboardData
这些是“本场比赛状态”，新房间应该干净。

4.2 global profile 长期资料层
为了避免新房间丢掉昨天设置好的预设，项目需要全局资料层：
fries_cup_profile_v1
应该属于全局资料的内容：
rosterPresetLibraryteamPresetscastersstaffMembersshortcutsvideoLibraryhighlightLibrarycoverCasterscoverAdmins
目标逻辑：
新房间 = 默认比赛数据 + 全局资料层
也就是：
比分 / 地图 / BAN / 当前画面 = 干净队伍预设 / 解说 / 快捷键 / 素材库 = 继承昨天

5. 主要模块
5.1 App.jsx
核心职责：
区分控制台和 #overlay维护 Program / Preview注册所有 scene提供 MatchContext处理 takeScene处理 overlay 订阅渲染 Program Monitor / Preview Monitor / Overlay 输出
关键原则：
App.jsx 是 globalScene 合法写入点之一Overlay 页面只读 Program 状态控制台页面负责编辑和 TAKE

5.2 ConsoleWorkspace.jsx
控制台主布局。
职责：
顶部状态栏Broadcast Room 房间弹窗Preview / Program 双监视器场景选择器Auto-Take Deck当前编辑器切换快捷操作右侧状态栏 / 快照 / 日志
当前还需要持续推进的方向：
完善中英文切换Broadcast Room 弹窗全部 i18n顶部 ROOM 状态 i18nCOPY / COPIED / OBS URL 等硬编码替换

5.3 LiveEditor.jsx
比赛实时控制核心。
职责：
A/B 队伍实时信息比分控制胜负结算当前五人攻防状态实时 BAN 位语音监听状态自动入场触发选手名牌触发BAN 页面触发底部 ticker 控制
重要逻辑：
LiveEditor 不应该直接写 globalScene结算画面应先写 winnerScene / winnerSide，再 takeScene('WINNER')BAN 页面应读取当前 bansA / bansB
未来可讨论项：
StartingLineupScene 的控制按钮位置可能放在 LiveEditor 的导播核心控制区，但当前暂定

5.4 StartingLineupScene.jsx
双方首发阵容展示页。
它读取：
matchData.playersAmatchData.playersBmatchData.rosterPlayersAmatchData.rosterPlayersB
本质是：
当前 A/B 队场上五人展示
当前没有独立控制台，合理做法是后续在 LiveEditor.jsx 增加按钮：
双方首发首发阵容双方首发上墙
但位置暂定，因为中间控制区已经很挤。

5.5 TeamDBEditor.jsx
战队和选手总库。
职责：
导入 / 导出队伍数据维护 rosterPresetLibrary导入八强名单保留经理 / 教练维护队伍预设
它属于“长期资料”，未来应纳入 global profile。

5.6 RosterEditor.jsx / RosterScene.jsx
队伍阵容展示模块。
职责：
选择 A/B 队读取 rosterPlayersA / rosterPlayersB展示选手、职责、英雄立绘上墙到 ROSTER
上墙标准逻辑：
写 liveRosterTeamsetPreviewScene('ROSTER')takeScene('ROSTER')

5.7 MapPoolEditor.jsx / MapPoolScene.jsx
地图池模块。
职责：
赛制 BO3 / BO5 / BO7当前地图地图模式选图 / BAN / 胜者实时 BAN 位归档到地图地图池展示模式
重要逻辑：
当前地图切换时同步顶层 bansA / bansBMapPoolScene 纯展示

5.8 BanPhaseScene.jsx
BAN 页面。
职责：
读取当前 bansA / bansB读取 banOrderMode显示双方 BAN 英雄显示地图 / 阶段 / 队伍
它应该只读 matchData，不反写状态。

5.9 MatchLiveHUD.jsx
比赛实况 HUD。
职责：
显示比分队名队标当前地图当前地图序列实时 BAN 位选手名牌 / 自动入场 overlay
已修过的关键问题：
重置后默认 BAN 不应该变成 D.Va / Tracer默认应为 damage/tbd

5.10 CasterEditor.jsx / CasterScene.jsx
解说 / 赛管 / 采访框模块。
职责：
解说头像解说名称角色标题社交账号采访框具体选手发言队伍代表发言全队发言
未来改进：
头像资源下拉选择从 src/assets/casters 或 public/assets/casters 自动识别

5.11 StatsEditor.jsx / StatsScene.jsx / OcrScannerModal.jsx
数据页模块。
职责：
OCR 识别比赛截图校对选手数据显示比赛数据板数据模板开关
关键点：
StatsScene 是纯展示层OcrScannerModal 是本地 OCR 处理StatsEditor 可用 processImageForStorage 存图

5.12 CountdownEditor.jsx / CountdownScene.jsx
倒计时模块。
职责：
倒计时文字倒计时视频模式赛程预告板视频播放列表
已定原则：
CountdownScene 不应该反写 activeVideoPath视频播完后的本地切换只放组件 state

5.13 VideoEditor.jsx / VideoScene.jsx
视频播放模块。
职责：
视频素材库播放列表当前视频网页播放 / OBS 本地模式
已定原则：
VideoScene 不反写 matchData视频结束只做本地播放循环 / 下一条

5.14 HighlightEditor.jsx / HighlightScene.jsx
高光回放模块。
职责：
高光素材库播放队列当前高光静音状态网页播放 / OBS 本地模式
已去掉 OBS WebSocket 强依赖。
未来 OBS WebSocket 可以作为增强，不作为高光同步主链路。

5.15 CoverEditor.jsx / BroadcastCoverScene.jsx
直播封面模块。
职责：
通用封面对阵封面品牌封面导出封面图片
ConsoleWorkspace 内部通过隐藏的 BroadcastCoverScene + html2canvas 导出 PNG。

6. 数据包装模块 DataGraphics
6.1 DataGraphicsEditor.jsx
数据包装入口。
职责：
读取外部数据 DB缓存数据源切换不同图文面板给各 panel 提供 db / dbStatus / 当前比赛队伍
它不是 scene，而是控制台里的图文编辑中心。

6.2 LeaderboardPanel.jsx / LeaderboardScene.jsx
排行榜模块。
职责：
选手排行全部 / 八强筛选指标切换输出 leaderboardData
上墙：
LEADERBOARD_SCENE

6.3 MapProfilePanel.jsx / MapProfileScene.jsx
地图剖析模块。
职责：
按地图类型 / 地图名筛选自动优先当前比赛两队计算双方地图表现输出 mapProfileData
已处理问题：
PENALTY / FORFEIT / BYE 不应该出现在地图类型下拉Hanaoka / Clash 地图路径需要修正

6.4 PlayerComparisonPanel.jsx / PlayerComparisonScene.jsx
选手对位模块。
职责：
选择 A/B 队选择 A/B 选手自动填充坦克 / 输出 / 辅助对位输出 playerComparisonData
上墙：
H2H_SCENE

6.5 PlayerSpotlightPanel.jsx / PlayerSpotlightScene.jsx
焦点选手模块。
职责：
选择八强 / 全部选择队伍选择选手按数据职责生成选手资料卡生成五项指标生成雷达图数据输出 playerSpotlightData
上墙：
MVP_SCENE

6.6 TeamComparisonPanel.jsx / TeamComparisonScene.jsx
队伍对比模块。
职责：
自动识别当前比赛两队计算全队 OVERALL计算 TANK / DPS / SUP 职责表现支持输出类别切换队标读取 src/assets/logos输出 teamComparisonData
上墙：
TEAM_COMPARISON_SCENE

7. 场景命名约定
当前主要 scene key：
LIVEMAP_POOLROSTERSTATSCASTERSCOUNTDOWNHIGHLIGHTVIDEOCOVERWINNERDATA_GRAPHICSMVP_SCENEH2H_SCENETEAM_COMPARISON_SCENEMAP_PROFILE_SCENELEADERBOARD_SCENESTARTING_LINEUP_SCENE
推荐规则：
主功能页：LIVE / STATS / ROSTER图文输出页：XXX_SCENE

8. i18n 中英文系统
项目使用：
react-i18nextsrc/locales/en/translation.jsonsrc/locales/zh/translation.json
目前已经覆盖大量模块：
workspacerightSidebarcasterEditorcountdownEditorcoverEditorhighlightEditorliveEditormapPoolEditordataGraphicsPanels
仍需持续清理：
ConsoleWorkspace.jsx 中 Broadcast Room 弹窗硬编码部分 COPY / COPIED / ROOM / OBS URL 文案部分 scene 内部英文系统字样部分 DataGraphics scene 的固定英文 UI
未来做通用赛事版时，i18n 应该变成基础能力。

9. 样式与布局系统
项目有统一样式系统：
src/constants/stylessrc/utils/editorUi.js
支持不同密度：
standardcompactultraspacious
createEditorUi() 负责统一生成：
inputselectbtnoutlineBtnactionBtnpanelBodystackinline2inline3smallTextlabelText
控制台布局目标：
高信息密度适配 1080P / 2K / 4K适合 OBS Dock / 浏览器窗口工业风 / 黑黄白尽量减少灰色

10. 资源系统
当前资源路径大致如下：
public/assets/heroes/tank/*.pngpublic/assets/heroes/damage/*.pngpublic/assets/heroes/support/*.pngpublic/assets/maps/Control/*.jpgpublic/assets/maps/Escort/*.jpgpublic/assets/maps/Hybrid/*.jpgpublic/assets/maps/Push/*.jpgpublic/assets/maps/Flashpoint/*.jpgpublic/assets/maps/Clash/*.jpgsrc/assets/logos/*.pngsrc/assets/casters/*.jpg
当前问题：
英雄图片路径解析分散在多个 scene / panel地图背景解析也分散队标解析有些文件用 public，有些用 src/assets
未来通用版建议抽象：
src/utils/heroResolver.jssrc/utils/mapAssetResolver.jssrc/utils/teamLogoResolver.js

11. 已经确定的工程原则
11.1 场景组件原则
所有 scene 应该是纯展示层：
不 updateData不 takeScene不写 globalScene不写 localStorage不依赖 useOBS
允许：
useState 做动画状态useState 做图片 fallbackuseMemo 处理展示数据

11.2 控制面板原则
Panel / Editor 可以写数据，但不能直接写 globalScene。
标准上墙方式：
updateWithHistory('Set Something Data', {  ...matchData,  somethingData: payload,  dataGraphics: {    type: 'SOMETHING',    payload  }});setPreviewScene?.('SOMETHING_SCENE');window.setTimeout(() => {  takeScene?.('SOMETHING_SCENE');}, 0);

11.3 Undo / Snapshot 原则
Undo 和 Snapshot 不应该恢复旧 Program scene。
也就是：
可以恢复数据不应该恢复 globalScene
否则会出现：
点 Undo↓Program 被回滚到旧画面↓Overlay 跟着跳回旧 scene

12. 当前已解决 / 已讨论过的问题
12.1 Program 和 Overlay 不同步
核心原因曾经是：
旧 payload / stale payload 回流覆盖新 Program
已确定解决方向：
payload timestamproom 校验忽略旧 payloadProgram 只认最新 globalScene

12.2 刷新后数据丢失
原因：
room-scoped 存储导致新 room 不继承旧资料
解决方向：
room data + global profile 双层存储

12.3 OBS Dock 白屏
判断：
OBS Custom Browser Dock 内置 Chromium 不稳定完整 React 控制台长期运行可能白屏
当前实际策略：
能跑通即可正式先用 OBS Dock 控制台 + OBS Overlay白屏时重开 Dock 或重启 OBS未来再考虑云端同步 / 外部 Chrome 控制台

12.4 StartingLineupScene 没控制台
判断：
它读取 LiveEditor 当前场上五人不需要独立 StartingLineupEditor按钮应该放 LiveEditor
但因为 LiveEditor 中间区域已经很挤，目前暂定。

13. 通用赛事版改造方向
未来要做“通用赛事版”，应该把项目从“守望先锋定制工具”抽象成：
Tournament Broadcast Console
13.1 应抽象的内容
赛事名称主视觉颜色LOGO队伍数据库选手数据库角色系统地图 / 赛制系统数据指标系统Overlay 场景模板语言包OBS 接入方式

13.2 守望先锋专属内容需要插件化
当前强绑定 OW 的内容：
英雄职责：TANK / DPS / SUPPORT地图类型：CONTROL / ESCORT / HYBRID / PUSH / FLASHPOINT / CLASHBAN 英雄每 10 分钟数据OW 风格 HUD
通用版应改成：
gameConfigroleConfigmapConfigstatConfigassetConfigsceneTemplateConfig
例如：
gameConfig = {  gameId: 'overwatch2',  roles: ['TANK', 'DPS', 'SUPPORT'],  maps: [],  stats: [],  assets: {}};

13.3 通用赛事版模块保留
这些模块可以保留为通用赛事能力：
LiveEditorTeamDBEditorCasterEditorRosterEditorMapPoolEditorCountdownEditorVideoEditorHighlightEditorCoverEditorDataGraphicsEditorRightSidebarConsoleWorkspace
但需要把文案和数据结构从 OW 中解耦。

14. 下一阶段优先级建议
当前最合理的优先级：
1. 稳定现有 OBS Dock + Overlay 单机方案2. 完成 global profile 双层存储3. 清理 ConsoleWorkspace i18n 硬编码4. 整理 hero / map / logo resolver5. 补齐 StartingLineupScene 控制入口6. 继续检查所有 scene 是否保持纯展示层7. 再考虑 OBS WebSocket 作为可选增强8. 最后再考虑云端 sync-server

15. 一句话总结
这个项目现在是一个 以 React 控制台为核心、通过 room 隔离和 Program/Preview/TAKE 机制驱动 OBS Overlay 的电竞赛事导播系统。当前版本优先保证单机 OBS 内可用；未来可以通过抽象游戏配置、素材解析、数据指标和场景模板，发展成一套通用赛事导播工具。